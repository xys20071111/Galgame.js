const app = new PIXI.Application({height:600,width:800});
document.body.appendChild(app.view);
const SplendUI ={
    background: null,
    bgm : null,
};
const GameUI = {
    bgm : null,
    background : null,
    name : null,
    text : null
}

